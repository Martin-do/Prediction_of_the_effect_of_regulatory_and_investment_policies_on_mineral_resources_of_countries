# Version notes — V0.4.3

V0.4.3 is a manuscript-alignment and reporting release. The scientific analysis remains the locked V0.4.2 analysis.

## Locked core retained

- same archived WDI and WGI workbooks;
- same 217-economy, 1990–2024 corrected panel;
- same M1–M3 feature ladder;
- same country folds, future cutoffs and matched rows;
- same Ridge, Elastic Net and Random Forest settings;
- same strongest-benchmark rule;
- same paired country-cluster bootstrap and seeds;
- same five-way unit, setup and cross-setup rules;
- same final setup-dependent classification for both signals.

## New reporting layer

The release makes the manuscript's base-model benchmark-margin claim directly traceable to generated unit-level and headline outputs. It also generates the manuscript's Tables 1, 3 and 4 source files and the Figure 2 threshold source from the locked analytical outputs.

## WGI scale transparency

The primary Regulatory Quality input remains the WGI 2025 Revision governance estimate. The alternative 0–100 score is tested only in an isolated post hoc implementation check. The two representations are affine-equivalent in the frozen workbook to numerical precision and produce no verdict changes under shared bootstrap draws.

## Validation status

The V0.4.3 analytical comparison passes at 203/203: 202 inherited V0.4.2 files remain byte-identical and one new unit-level base-margin output reproduces byte for byte. Six remaining manuscript-reporting outputs also reproduce byte for byte.
