# Legacy repository snapshot — not used for V0.4.1 results

This directory preserves the files that were previously exposed at the root of the GitHub repository.
They include developmental datasets, scripts, notebooks and outputs from the superseded stock-era pipeline.

**Do not use these files to reproduce or interpret the locked V0.4.1 findings.**

The authoritative build starts at the repository root with:

```bash
python run_pipeline.py
```
